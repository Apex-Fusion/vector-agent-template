/**
 * supplier/src/routes/status.ts — GET /status handler.
 *
 * Returns { status, current_escrow_ref?, last_seen } from SupplierState.snapshot().
 * Always 200. Never touches chain. current_escrow_ref only present when working.
 *
 * Stub — throws until M1-C-green implementation lands.
 */

export {};
